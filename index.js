const fs = require('fs');
const path = require('path');

module.exports = {
    book:{
        assets: "./assets",
        css: ["style.css"],
        js: ["inter.js"]
    },
    // Map of hooks
    hooks: {},

    // Map of new blocks
    blocks: {

    },

    // Map of new filters
    filters: {}
}